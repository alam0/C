//graph.c
#include <stdio.h>
#include <math.h>
int main ( void )
{
	//Initialize variables
	float x;
	float y;
	int pound;
	
	//Create graph
	printf("A plot of y = 3 + 3cos(x).\n"); 
	printf("X\tY\n");
	for(x = 0.00; x <= 30.20; x = x + 0.10 ){
	y = (5*(1+cos(x));
	pound = y + 1;
	printf("%5.2f\t%5.2f\n", x, y);
	}
	printf("The maximum of");
	printf("The minimum of"); 
return 0;
}
